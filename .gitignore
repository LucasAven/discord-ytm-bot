# Secretos: nunca al repo
.env
browser.json
oauth.json

# Python
.venv/
__pycache__/
*.pyc

# Notas locales, no van al repo
backlog.md

# Salida de PyInstaller y el ffmpeg que se baja para compilar
build/
dist/
ffmpeg
ffmpeg.exe
ffmpeg.zip
ffmpeg-tmp/
